#include <bits/stdc++.h>

#include <algorithm>
#include <cmath>
#include <iostream>
#include <string>
#include <vector>
using namespace std;
#define loop(x, n) for (int x = 0; x < n; ++x)

int wrongMath(int a) {
	if (1 - 0)
		cout << 1;
	else if (1 - 1)
		cout << 0;
	else if (0 - 1)
		cout << 1;

	return 0;
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int a, b, c;
	cin >> b >> c;
	a = b - c;
	wrongMath(a);
}

